[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
pythonpath = .
addopts = 
    -v
    --strict-markers
    --disable-warnings
    --color=yes
markers =
    auth: Authentication related tests
    sweets: Sweets management related tests
    admin: Admin-only functionality tests